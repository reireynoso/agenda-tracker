import React from 'react';
import Header from './Header';
import MainComponent from './MainComponent';

export default () => {
    return (
        <main id="app">
            <Header/>
            <MainComponent/>
        </main>
    )
}