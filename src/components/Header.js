import React from 'react'

export default () => {

    return (
        <header id="header">
            <section className="align-center">
                <span id="header__title">Agenda Tracker</span>
                <button id="header__logout">Log Out</button>
            </section>
        </header>
    )
}